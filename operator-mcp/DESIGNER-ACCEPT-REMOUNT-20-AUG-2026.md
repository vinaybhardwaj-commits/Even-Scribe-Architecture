# Designer acceptance — remount resume
20 Aug 2026. Scribe Designer. For the orchestrator.

Read `REMOUNT-ACCEPTANCE-20-AUG-2026.md` (`47d6c6c` … `10f73cf`, slice 5 `592213d`). Preview only. `main` untouched.

## Call

**Remount is accepted.** Item 1 of the 20 Aug order is closed, with the two notes below.

| Row | Designer |
|---|---|
| A1–A4, A7–A11 | Accepted. A9 and A10 (the 19 Aug reload-spoiled rows) are closed. |
| A5 | Accepted on outcome (start screen, no rejoin). The check landed after the janitor, so the reason was `none` not `stalled`. Stalled itself stays on unit tests. Do not re-stage a 10–30 min wait unless Friday’s day makes one naturally. |
| A6 | Accepted as deferred. First reload of pilot day two against a real previous-day session is the right proof. Send that row when it exists. |

## Ratified beyond the original table

D1–D6 already stood. The four “code made unavoidable” points in this report match them. Stall clock / janitor still imported from `bench-reaper-core`. No second window.

Slice 5: instant reload marker (15 s, room-scoped, deleted on first read), honest screens, client-stamped handover events. Accepted, including the eleven-line active-route edit so `at` is client wall clock (0043).

## Leave

- Kiosk MB still this-tab-only after rejoin. Count is honest. Do not add sizes to the resume answer for this.
- Cross-machine handover clock skew: slower, never wrong. Same fail-safe class. Do not add a server round-trip for this.
- `kiosk_remount_resumed` with `brain_status: none`. Correct. Fuse treats it as tape quality, not a failed hop.

## Dead links (my side)

`EVEN-SCRIBE-MCP-PRD-19-AUG-2026.md` and `EVEN-SCRIBE-FUSE-PRD-19-AUG-2026.md` belong in `Even-Scribe-Architecture`, not the product repo. I will put them there. Do not copy design into `Even-Transcription-Assistant` to fix a designer link.

## Next (unchanged order)

Replay dry-run of `scribe_replay_session` on `bs_xvntaugh`. Cue list only. Post nothing. Then scratch write. Merge still waits on Vinay’s word about the 19 Aug tapes.
