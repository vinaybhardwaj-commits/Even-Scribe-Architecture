# Designer close — fuse programme §11
22 Aug 2026. Scribe Designer. For the orchestrator.

Read `BUILDER-REPORT-PROGRAMME-CLOSE-22-AUG-2026.md`. Live `main` `9010ba8`. I checked: health sha matches, live OPD 7 is still `cue_95z4avnv`, scratch OPD 7 scoreboard now reads `opened 13 / closed 12 / moved 4 / unbound 10` = 39. The four fields sum. No individual ids in this note.

## Call

**§11 is closed.** Slices 1–5 delivered. Slice 6 stays skipped. Live days untouched on every write of the programme. Arm A is the default. B and C stay disqualified. Promotion is not implied by finishing.

The bake-off finding stands: the model’s wrong answer was tidier. Opening-evidence is why we could see that. Tape and warehouse on 19 Aug barely overlap, and that is now a scoreboard, not a claim.

The four errors you listed (type names, empty payload, missing `calendar_uid`, silence-as-interior-only) are why we read production back. Keep doing that.

## What is next, in this order

**1. A second clinic day.** Highest value, and not a build. Every constant on the scoreboard was chosen against one light day. I will not slide `LAST_MARK_WINDOW_MS` or the silence threshold until a second tape exists. You record. I do not invent a day.

**2. Speech turns.** Agree this is the largest product gap. `stt_turn` and `speaker_match` are named in `0042` and in §9, and nothing emits them. Voice cannot be a prior with no turns. I will write that brief when asked — replay Mini Whisper on a finished Bench tape, post `stt_turn` onto **scratch only**, `speaker_match` later and weak. Not the live 250 ms ear. Not a second recorder. Not this close.

**3. Promotion, still later.** Which day, which arm, what happens to a live cue that already exists, and how a promoted visit is marked so it is not mistaken for a live fuse write. Not this weekend. Not because §11 finished. Live OPD 7 is one mark and a `whole_day` warehouse silence. Scratch is a reconstructed warehouse-shaped day. Two pictures.

## Still in force

- Do not write the live `room_day`.
- Do not re-run B or C. Do not add a run id to `visit`.
- JSON behind the MCP door is the artefact. No clinician page. No room screen.
- `LAST_MARK_WINDOW_MS` stays 45 minutes.
- No Pulse writes. No Slack. No Gerrit.

The V operator note and spin-up prompt match this close. I am not rewriting them.
